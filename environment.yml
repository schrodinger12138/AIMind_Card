name: ai_cards
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  # PyQt6相关 - 使用conda安装以避免Windows DLL问题
  - pyqt>=6.0.0
  - pyqtwebengine>=6.0.0
  # pip依赖
  - pip
  - pip:
    - openai>=1.0.0
    - pyperclip>=1.8.0
    - PyMuPDF>=1.23.0
    - Pillow>=10.0.0
    - requests>=2.31.0
    - tqdm>=4.66.0
    - markdown>=3.4.0
    - Pygments>=2.15.0
    - matplotlib>=3.7.0
    - weasyprint>=60.0
    - jinja2>=3.1.0

